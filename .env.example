# Copy to .env and fill in. Never commit .env.
ORS_API_KEY=
# Optional — only used to disambiguate outdoor-space classification.
ANTHROPIC_API_KEY=
# Optional — Nominatim asks for a contact UA; default is fine.
# NOMINATIM_USER_AGENT="london-rentals/1.0 (you@example.com)"
